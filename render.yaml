services:
  - type: web
    name: simple-plans-tracker
    runtime: static
    buildCommand: npm run build
    staticPublishPath: .
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
